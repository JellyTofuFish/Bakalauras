{% extends 'base.html.twig' %}

{% block title %}New Question{% endblock %}

{% block body %}

    {% trans_default_domain 'question' %}

    <h1>Create new Question</h1>

    {{ include('question/_form.html.twig') }}

    <a href="{{ path('question_index') }}">{{ 'button.back'|trans({}, 'question') }}</a>
{% endblock %}
