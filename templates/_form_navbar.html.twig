
<nav class="navbar navbar-expand-lg navbar-dark bg-primary border-bottom shadow-sm bd-navbar">
    <a class="navbar-brand" href="{{ path('question_index') }}">ktu-logo</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarMainBar" aria-controls="navbarMainBar" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarMainBar">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item px-2 m-auto">
                {% if not app.user %}
                <button type="button" class="btn btn-outline-light dropdown-toggle" data-toggle="dropdown" data-display="static" aria-haspopup="true" aria-expanded="false">{{ 'button.login'|trans }}</button>
                {{ include('security/login.html.twig') }}
                {% else  %}
                    <a type="button" class="btn btn-outline-light" href="{{ path('app_logout') }}">{{ 'button.logout'|trans }}</a>
                {%  endif %}
            </li>
        </ul>
    </div>
</nav>